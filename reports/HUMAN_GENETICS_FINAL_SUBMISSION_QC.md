# Human Genetics final submission QC

1. Comparator terminology: `RESOLVED` — primary Fisher comparator is now eligible non-DAR retinal OCR comparator; matched terminology is restricted to matched permutation/strata.

2. DAR/OCR set identity: `RESOLVED` — 380,615 - 1,069 - 267 = 379,279.

3. Permutation universe: `RESOLVED` — full pool includes DAR plus eligible non-DAR OCR; informative strata retain 1,069 DAR and 59,215 eligible non-DAR OCR SNPs. OCR status is explained because 163 source-defined DAR SNPs are outside broad OCR.

4. Citation logic: `PASS` — Hysi, Verhoeven and Cheng roles are separated; Brown and Martin are cited where used.

5. Listed-but-uncited references: `none`.

6. Numeric consistency: `PASS`.

7. Symbol rendering: `PASS` — PDF text extraction and visual PNG inspection confirmed χ²het, h², r², α and EUR–EAS rendering.

8. Remaining placeholders: `NONE`.

9. Remaining scientific risks: small DAR denominator; modest/local effects unresolved; public summary-statistics design; source-effect scale heterogeneity; author and submission-system declarations still require author confirmation.

Final verdict: `SUBMISSION_READY`.
